{
  "name": "backend",
  "version": "0.1.0",
  "private": true,
  "description": "Slipway API and scheduled corridor poller. Not published to npm.",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "build": "tsc --build",
    "test": "vitest run --root=.. backend",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.0",
    "@hono/zod-validator": "^0.4.1",
    "@slipwaykit/adapter-mock": "workspace:*",
    "@slipwaykit/adapter-sep24": "workspace:*",
    "@slipwaykit/core": "workspace:*",
    "@stellar/stellar-sdk": "^13.0.0",
    "better-sqlite3": "^11.5.0",
    "drizzle-orm": "^0.36.0",
    "hono": "^4.6.0",
    "node-cron": "^3.0.3",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.11",
    "@types/node-cron": "^3.0.11",
    "tsx": "^4.19.0"
  }
}
